import React from "react";
import { MoonLoader } from "react-spinners";

const Loading = () => (
  <div className="spinner">
    <MoonLoader color="#EF4444" size={40} />
  </div>
);

export default Loading;
